label: "Multi-robot FastDDS (UDP)"
description: "Several robots on one LAN; FastDDS over UDP. No router needed; each robot a unique namespace."
fields:
  RMW_IMPLEMENTATION: rmw_fastrtps_cpp
  RMW_PROFILE: udp                # the per-node default this preset cascades
  ROS_DOMAIN_ID: "0"
  ROS_AUTOMATIC_DISCOVERY_RANGE: SUBNET
